import { Box, Typography } from "@mui/material";
import React from "react";

const App = () => {
    return (
        <Box>
            <Typography>App</Typography>
        </Box>
    );
};

export default App;
